# Activation Command:
source venv/bin/activate
# Deactivation Command:
deactivate

1. PEPs -> Python Enhancement Proposals
2. Everything in Python is an object and object have functions we call methods which we can access using dont (.) notation.
3. Falsey values:
    "" Empty string
    0 
    None
4. In Python we have primitive types such as numbers, string, boolean and complex types such as range().
5. (1,2,3,4) => this is called tuples and [1,2,3,4] => this is called list. The difference is we cant modify tuples.

Numpy:

6. This is simple to understand, look inside array we give row and column index like this array[0,0] so we separate row and column by comma (,) so we have start:end:step too and we can use this for both rows and columns start means starting index for wherever using like for row or column then end means the position this will exact return till whatever value you type.
7. array.shape() tell how much rows and columns are there.
8 To broadcast the arrays they must have same rows and columns or 1 of the rows or columns must be 1.
 